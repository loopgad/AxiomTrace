name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  host-c:
    name: Host ${{ matrix.compiler }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        compiler: [gcc, clang]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install clang-tidy
        if: matrix.compiler == 'clang'
        run: sudo apt-get update && sudo apt-get install -y clang-tidy
      - name: Configure
        run: >
          cmake -S . -B build -G Ninja
          -DAXIOM_PLATFORM=host
          -DAXIOM_BUILD_TESTS=ON
          -DAXIOM_BUILD_EXAMPLES=ON
          -DAXIOM_SELFTEST=ON
          -DAXIOM_ENABLE_CLANG_TIDY=OFF
          -DCMAKE_C_COMPILER=${{ matrix.compiler }}
      - run: cmake --build build --parallel
      - run: ctest --test-dir build --output-on-failure
      - name: Configure clang-tidy core gate
        if: matrix.compiler == 'clang'
        run: >
          cmake -S . -B build-clang-tidy -G Ninja
          -DAXIOM_PLATFORM=host
          -DAXIOM_BUILD_TESTS=OFF
          -DAXIOM_BUILD_BENCHMARKS=OFF
          -DAXIOM_BUILD_EXAMPLES=OFF
          -DAXIOM_ENABLE_CLANG_TIDY=ON
          -DCMAKE_C_COMPILER=clang
      - name: Build clang-tidy core gate
        if: matrix.compiler == 'clang'
        run: cmake --build build-clang-tidy --parallel

  sanitizers:
    name: ASan + UBSan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: >
          cmake -S . -B build-san -G Ninja
          -DAXIOM_PLATFORM=host -DAXIOM_BUILD_TESTS=ON -DAXIOM_BUILD_EXAMPLES=ON
          -DCMAKE_C_COMPILER=clang
          -DCMAKE_C_FLAGS="-fsanitize=address,undefined -fno-omit-frame-pointer"
      - run: cmake --build build-san --parallel
      - run: ctest --test-dir build-san --output-on-failure

  performance-regression:
    name: Host P99.9 regression
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Select baseline commit
        id: baseline
        shell: bash
        run: |
          base="${{ github.event.pull_request.base.sha || github.event.before }}"
          if ! git cat-file -e "${base}^{commit}" 2>/dev/null; then
            base="HEAD^"
          fi
          echo "sha=${base}" >> "$GITHUB_OUTPUT"
      - name: Build candidate benchmark
        run: |
          cmake -S . -B build-benchmark -G Ninja \
            -DAXIOM_PLATFORM=host -DAXIOM_BUILD_TESTS=ON \
            -DAXIOM_BUILD_BENCHMARKS=ON -DAXIOM_BUILD_EXAMPLES=OFF \
            -DCMAKE_C_COMPILER=clang
          cmake --build build-benchmark --target test_benchmark --parallel
      - name: Build baseline benchmark
        run: |
          git worktree add --detach ../axiomtrace-baseline "${{ steps.baseline.outputs.sha }}"
          cmake -S ../axiomtrace-baseline -B ../axiomtrace-baseline-build -G Ninja \
            -DAXIOM_PLATFORM=host -DAXIOM_BUILD_TESTS=ON \
            -DAXIOM_BUILD_BENCHMARKS=ON \
            -DAXIOM_BUILD_EXAMPLES=OFF -DCMAKE_C_COMPILER=clang
          cmake --build ../axiomtrace-baseline-build --target test_benchmark --parallel
      - name: Enforce P99.9 regression limit
        run: |
          python scripts/benchmark_gate.py \
            --baseline ../axiomtrace-baseline-build/tests/host/test_benchmark \
            --candidate build-benchmark/tests/host/test_benchmark \
            --runs 11 --threshold-percent 20 \
            --output benchmark-report.json
      - uses: actions/upload-artifact@v4
        with:
          name: host-benchmark-report
          path: benchmark-report.json

  python-tooling:
    name: Python 3.12 tooling
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version-file: tool/.python-version
      - uses: astral-sh/setup-uv@v5
      - run: uv sync --project tool --extra test --locked
      - run: uv run --project tool --extra test python -m pytest -q
      - run: uv run --project tool --extra test python -m compileall -q tool/src tests
      - run: uv run --project tool --extra test python tool/golden/update_golden.py --check
      - run: uv run --project tool --extra test axiom-validate --golden tool/golden
      - run: uv run --project tool --extra test python scripts/release_checks.py

  cross-compile:
    name: GNU reference compile + resources
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install cross compilers
        run: |
          sudo apt-get update
          sudo apt-get install -y ninja-build gcc-arm-none-eabi gcc-riscv64-unknown-elf picolibc-riscv64-unknown-elf
      - name: ARM preset builds and budgets
        run: |
          for preset in tiny prod field dev; do
            cmake -S . -B "build-arm-${preset}" -G Ninja \
              -DAXIOM_PLATFORM=cortex-m \
              -DAXIOM_PRESET="${preset}" -DAXIOM_BUILD_TESTS=OFF -DAXIOM_BUILD_EXAMPLES=OFF \
              -DCMAKE_BUILD_TYPE=MinSizeRel -DCMAKE_TRY_COMPILE_TARGET_TYPE=STATIC_LIBRARY \
              -DCMAKE_C_COMPILER=arm-none-eabi-gcc \
              -DCMAKE_C_FLAGS="-mcpu=cortex-m4 -mthumb -fstack-usage"
            cmake --build "build-arm-${preset}" --parallel
            python scripts/resource_report.py --build-dir "build-arm-${preset}" \
              --size-tool arm-none-eabi-size --preset "${preset}" --enforce \
              --output "resource-reports/${preset}.json"
          done
      - name: RISC-V compile-only
        run: |
            cmake -S . -B build-riscv -G Ninja \
              -DAXIOM_PLATFORM=riscv -DAXIOM_PRESET=prod \
            -DAXIOM_BUILD_TESTS=OFF -DAXIOM_BUILD_EXAMPLES=OFF \
            -DCMAKE_BUILD_TYPE=MinSizeRel -DCMAKE_TRY_COMPILE_TARGET_TYPE=STATIC_LIBRARY \
            -DCMAKE_C_COMPILER=riscv64-unknown-elf-gcc \
            -DCMAKE_C_FLAGS="-march=rv32imac_zicsr -mabi=ilp32 -fstack-usage -isystem /usr/lib/picolibc/riscv64-unknown-elf/include"
            cmake --build build-riscv --parallel
      - uses: actions/upload-artifact@v4
        with:
          name: arm-resource-reports
          path: resource-reports

  release-artifact:
    name: Release single header
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Read project version
        id: project-version
        shell: bash
        run: |
          version="$(python -c 'import re,sys; from pathlib import Path; text=Path("CMakeLists.txt").read_text(encoding="utf-8"); match=re.search(r"project\(AxiomTrace VERSION ([0-9.]+)", text); sys.exit("project version not found") if match is None else print(match.group(1))')"
          echo "version=${version}" >> "$GITHUB_OUTPUT"
      - run: python tool/scripts/amalgamate.py
      - name: GNU11 implementation smoke
        run: |
          cat > /tmp/axiomtrace_impl.c <<'EOF'
          #define AXIOMTRACE_IMPLEMENTATION
          #include "axiomtrace.h"
          EOF
          cat > /tmp/axiomtrace_user.c <<'EOF'
          #include "axiomtrace.h"
          int main(void) { axiom_init(); AX_EVT(INFO, 1u, 1u, (uint8_t)1u); axiom_flush(); return 0; }
          EOF
          gcc -std=gnu11 -Wall -Wextra -Werror -Idist \
            /tmp/axiomtrace_impl.c /tmp/axiomtrace_user.c -o /tmp/axiomtrace-smoke
          /tmp/axiomtrace-smoke
      - uses: actions/upload-artifact@v4
        with:
          name: axiomtrace-${{ steps.project-version.outputs.version }}-single-header
          path: dist/axiomtrace.h
